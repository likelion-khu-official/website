# 내 프로젝트 — 검증 결과

> [pm/features/내프로젝트.md](../../features/내프로젝트.md) 의 성공 기준([pm/qa/criteria/일반사용자.yaml](../criteria/일반사용자.yaml)) 검증 결과.

> **범위**: 이 검증은 criteria의 given/when/then이 코드·API·화면에서 기대대로 동작하는지 — 기능 동작 기준 pass/fail만 판정합니다. **디자인 품질(비주얼 QA)·보안 침투 테스트·성능·접근성은 범위 밖**입니다.

## 익명 방문자 관점 — 요약

| ID | 결과 | 확인 방법 | 확인일자 |
|---|---|---|---|
| MYPROJ-001 | pass (PR #121, dev 미머지) | PR #121 코드 리뷰(머지 전) | 2026-07-19 |

**1/1 pass**

확인자: Claude(하네스) — 코드 정적 분석

## 근거

### MYPROJ-001 — pass

`gh pr diff 121`의 `ProjectController.java`(신규): `POST /api/projects`(1393-1394행)·`PATCH /api/projects/{id}`(1403-1404행)·`DELETE /api/projects/{id}`(1414-1415행) 전부 `@PreAuthorize("hasRole('MEMBER')")`. 수정·삭제 서비스 메서드는 `requireParticipant(id, memberId)`(1651행·1681행, `existsByProjectIdAndMemberId` 기반)를 호출해 "그 프로젝트 참여자 목록에 내가 있는가"를 확인한다 — 스펙의 "내가 참여한 것만 수정 가능"과 정확히 일치한다.

PR 본문 자체에 이 부분의 자체 리뷰 기록도 있다 — 최초 구현에서 "비번을 안 바꾼 멤버가 세 쓰기 엔드포인트를 전부 통과할 수 있었다"는 걸 발견해 `MemberPasswordGuardFilter`를 경로 나열 대신 "쓰기 메서드 여부" 기준으로 일반화해 고쳤다고 diff에 적혀 있다 — PR 안에서 자체적으로 잡고 고친 이력이라 별도 fail로 잡지 않는다.

익명 경계(인증 없이 세 메서드 호출 시 401)는 `SecurityConfig`가 `GET /api/projects`·`/api/projects/*`만 permitAll이라 나머지는 기본 `anyRequest().authenticated()`에 걸려 자동으로 성립한다(PROJ-006과 같은 근거).
