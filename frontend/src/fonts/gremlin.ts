import localFont from 'next/font/local';

// 임시 미리보기 전용 — 라이선스 미확인(All rights reserved, Muhammad Hastian).
// 실제 배포에는 정식 라이선스 구매 후 교체 필요.
export const gremlinTrial = localFont({
  src: './Gremlin-Regular.otf',
  variable: '--font-gremlin-trial',
  display: 'swap',
});
