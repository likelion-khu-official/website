-- 멤버가 자기 글만 조회·수정·삭제할 수 있도록 작성 당시 멤버 ID를 보관한다.
--
-- 기존 posts는 이름·파트 스냅샷만 있어 소유자를 안전하게 역추론할 수 없다. 같은 이름이 있을 수 있고,
-- 탈퇴 뒤에도 글은 남아야 하므로 이름으로 조인하지 않는다. 기존 행은 null(관리자만 상태 관리),
-- 이후 생성되는 글은 애플리케이션이 로그인 멤버 ID를 반드시 기록한다.

alter table posts add column author_member_id bigint;
