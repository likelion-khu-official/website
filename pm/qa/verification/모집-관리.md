# 모집 관리 — 검증 결과

> [pm/features/모집-관리.md](../../features/모집-관리.md) 의 성공 기준([pm/qa/criteria/일반사용자.yaml](../criteria/일반사용자.yaml)) 검증 결과.

> **범위**: 이 검증은 criteria의 given/when/then이 코드·API·화면에서 기대대로 동작하는지 — 기능 동작 기준 pass/fail만 판정합니다. **디자인 품질(비주얼 QA)·보안 침투 테스트·성능·접근성은 범위 밖**입니다.

## 익명 방문자 관점 — 요약

| ID | 결과 | 확인 방법 | 확인일자 |
|---|---|---|---|
| ADMRECRUIT-001 | pass (PR #126, dev 미머지) | PR #126 코드 리뷰(머지 전) | 2026-07-18 |

**1/1 pass**

확인자: Claude(하네스) — 코드 정적 분석

## 근거

### ADMRECRUIT-001 — pass

`gh pr diff 126`의 `RecruitmentManagementController.java`: 클래스 레벨 `@RequestMapping("/api/admin/recruitment")` + `@PreAuthorize("hasAnyRole('ADMIN','SUPER_ADMIN')")`(70-73행), `GET /status`(78행)·`PATCH /status`(84행). `SecurityConfig`는 이 경로를 permitAll하지 않으므로(모집-알림.md RECR-002에서 GET을 이미 확인) 익명은 인증 단계에서부터 401 — PATCH(실제 켜기/끄기 조작)도 같은 보호를 받는다.

미리보기(스펙의 "켜기 전 미리보기") 기능은 diff에서 별도 엔드포인트를 못 찾았다 — 이 부분은 아직 없는 것으로 보인다(note로만 남김, 별도 시나리오화는 다음 라운드).
