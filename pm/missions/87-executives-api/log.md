# 진행 로그 — #87 백엔드 · 운영진 소개 API

> PM이 판단·결정한 것만 시간순. GitHub 코멘트 복붙 아님.
> 형식: `## YYYY-MM-DD` 아래 일어난 일 / 결정 / 왜.

## 2026-07-09
- 발주 전, 운영진 소개를 **정적 서빙 vs DB**로 저울질. "연 1회 변경"은 활동계획과 운영진을 못 가르는 축이라 기각 → 실제 갈림길은 **사진(미디어) + 어드민 자기편집 surface**. 어드민에서 운영진을 사진 포함 직접 갈아끼운다는 전제를 PM이 확정 → **DB CRUD로 결정**(연 1회 개발자 커밋이면 static이 맞았을 것). 활동계획은 텍스트뿐이라 static으로 남김.
- 데이터 모델: #76 멤버 API와 **별개 엔티티**. 직책=자유 텍스트 + 정렬값(계층·시각은 프론트가 정렬값으로), 학번=입학 연도(예 22·24), 사진=기존 피드 이미지/OCI 스토리지 재사용.
