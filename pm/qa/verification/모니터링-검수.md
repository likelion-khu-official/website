# 모니터링 · 검수 — 검증 결과

> [pm/features/모니터링-검수.md](../../features/모니터링-검수.md) 의 성공 기준([pm/qa/criteria/일반사용자.yaml](../criteria/일반사용자.yaml)) 검증 결과.

> **범위**: 이 검증은 criteria의 given/when/then이 코드·API·화면에서 기대대로 동작하는지 — 기능 동작 기준 pass/fail만 판정합니다. **디자인 품질(비주얼 QA)·보안 침투 테스트·성능·접근성은 범위 밖**입니다.

## 익명 방문자 관점 — 요약

| ID | 결과 | 확인 방법 | 확인일자 |
|---|---|---|---|
| ADMMOD-001 | pass | 코드 정적 분석(dev 브랜치) | 2026-07-18 |
| ADMMOD-002 | fail | 코드 정적 분석(dev 브랜치) | 2026-07-18 |

**1/2 pass · 1 fail**

확인자: Claude(하네스) — 코드 정적 분석

## 근거

### ADMMOD-001 — pass — 글 숨김은 제대로 보호됨

`PostController.java:53-58`의 `PATCH /api/admin/posts/{id}/status`는 별도 `@PreAuthorize` 없이 `SecurityConfig.java:72`의 `.requestMatchers("/api/admin/posts/**", ...).authenticated()`에 걸린다 — permitAll이 없으므로 익명은 401 UNAUTHENTICATED.

### ADMMOD-002 — fail — 댓글 숨김은 인증 우회됨 (CMT-007과 동일 갭)

기대(then)는 글 숨김과 같은 401 보호인데, 실제로는 관리자용 댓글 숨김 API를 인증 없이 호출할 수 있다 — SecurityConfig 매처가 가리키는 경로와 컨트롤러 실제 경로의 불일치를 코드로 확인했다. 근거는 [`pm/qa/verification/블로그-익명댓글.md`](블로그-익명댓글.md)의 CMT-007과 동일 — 이 노드("모니터링·검수" = 관리자 권한 경계) 관점에서 재확인한 결과도 같다. 재현 상세(경로·설정 위치)는 공개 문서에 남기지 않는다([GUIDE](../GUIDE.md) 보안 규칙) — 비공개 전달.

**모니터링·검수 노드 안에서도 글 숨김과 댓글 숨김의 보호 수준이 다르다**는 게 이 재확인의 요점 — 같은 "관리자가 숨긴다"는 기능인데 한쪽은 안전하고 한쪽은 인증 우회 취약점이 있다.
