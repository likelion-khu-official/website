# 첫 로그인 비밀번호 변경 — 검증 결과

> [pm/features/첫로그인-비번변경.md](../../features/첫로그인-비번변경.md) 의 성공 기준([pm/qa/criteria/일반사용자.yaml](../criteria/일반사용자.yaml)) 검증 결과.

> **범위**: 이 검증은 criteria의 given/when/then이 코드·API·화면에서 기대대로 동작하는지 — 기능 동작 기준 pass/fail만 판정합니다. **디자인 품질(비주얼 QA)·보안 침투 테스트·성능·접근성은 범위 밖**입니다.

## 익명 방문자 관점 — 요약

| ID | 결과 | 확인 방법 | 확인일자 |
|---|---|---|---|
| PWFIRST-001 | pass (PR #121, dev 미머지) | PR #121 코드 리뷰(머지 전) | 2026-07-19 |

**1/1 pass**

확인자: Claude(하네스) — 코드 정적 분석

## 근거

### PWFIRST-001 — pass

`gh pr diff 121`의 `MemberAuthController.java`(diff) 54-61행: `@PreAuthorize("hasRole('MEMBER')") @PatchMapping("/password")` — 익명은 인증 자체가 안 되므로 이 단계에서 401 UNAUTHENTICATED로 막힌다.

참고로 같은 PR의 `MemberPasswordGuardFilter.java`(신규)가 "첫 로그인 후 비번을 안 바꾼 멤버는 이 엔드포인트(`/api/member/auth/**`) 외의 모든 쓰기 요청이 403 `MUST_CHANGE_PASSWORD`로 막힌다"는 강제 로직을 구현하는데, 이건 **로그인한 멤버 상태를 전제**로 해서 익명 관점 밖이다 — 멤버 관점 GWT가 생기면 그때 검증 대상.
