# 명단 열람 — 검증 결과

> [pm/features/명단-열람.md](../../features/명단-열람.md) 의 성공 기준([pm/qa/criteria/일반사용자.yaml](../criteria/일반사용자.yaml)) 검증 결과.

> **범위**: 이 검증은 criteria의 given/when/then이 코드·API·화면에서 기대대로 동작하는지 — 기능 동작 기준 pass/fail만 판정합니다. **디자인 품질(비주얼 QA)·보안 침투 테스트·성능·접근성은 범위 밖**입니다.

## 익명 방문자 관점 — 요약

| ID | 결과 | 확인 방법 | 확인일자 |
|---|---|---|---|
| ADMROSTER-001 | not-run | 코드 정적 분석(dev 브랜치) | 2026-07-18 |

**0/1 pass · 1 not-run**

확인자: Claude(하네스) — 코드 정적 분석

## 근거

### ADMROSTER-001 — not-run

지원자 명단: 지원폼 자체가 없어(지원폼.md 검증 참고) 지원자 데이터도, 조회 API도 없다.
구독자 명단: `backend/src/main/java/likelion/khu/website/notification/`을 확인했다 — `NotificationController`엔 `POST /api/notifications/subscribe` 하나뿐이고 관리자가 구독자 목록을 조회하는 GET 엔드포인트가 없다. `NotificationSubscription` 데이터는 DB엔 쌓이지만(PR #126의 `sendToAllSubscribers()`가 내부적으로 `findAll()`은 함) 이를 사람이 볼 수 있는 API·화면은 없다. 스펙대로 "미구현" — not-run.
