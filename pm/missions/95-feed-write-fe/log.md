# 진행 로그 — #95 피드 글쓰기 FE — 토큰 분기 제거·멤버 선택

> PM이 판단·결정한 것만 시간순. GitHub 코멘트 복붙 아님.

## 2026-07-10 — 매직링크 폐기로 방향 전환, FE 발주

- **토큰 분기(5개 화면)를 걷어내고 멤버 드롭다운(본인 선택)으로.** 목록·상세·댓글·이미지 업로드·임시저장·미리보기는 유지.
- **왜** — 매직링크 마찰 제거 + 이름 자유입력 대신 **멤버 명단 선택**(오타·비멤버 방지). 근거 PR #93 / `pm/docs/feed-write-policy.md`.
- **의존** — `GET /api/members`(#76) 선행. 미구현이면 착수 대기. 로스터는 멤버 몇 명 수동 시드로 언블록(#74 어드민 UI 안 기다림).
- **★ 어드민 검수 UI는 범위 밖**(#74 후 별도). 지금은 관리자가 API로 승인 — 정책 md "알려진 갭" 참조.
