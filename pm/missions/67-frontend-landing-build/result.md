# 결과 — #67 프론트 · 랜딩 실구현 — 시안의 전 섹션 + 정적 콘텐츠

> 미션이 닫힐 때 채운다. 재사용할 통찰은 pm/docs/learnings.md로 졸업.

## 산출물
- 랜딩 전 섹션 조립 — `page.tsx`가 Thumbnail·Introduce·IntroduceSession·Project·Members·Plan·Blog·Recruit·Footer. **Figma 규격 정합.** PR #42(뼈대)·#84(전 섹션)·fix `6c67cbc`. 데스크탑·모바일.
- 섹션 콘텐츠를 타입화한 `const`로 각 컴포넌트 상단에 분리(JSX 내 흩뿌림 아님). 라우트 정돈(projects·blog 전용 + 소개·운영진·활동은 랜딩 앵커).

## 결정
- 정적 콘텐츠 데이터 형태는 프론트가 정함. 동적화되는 항목이 생기면 그때 `shared/` 계약으로.
- 실데이터·사진은 운영진이 채움 — 이번은 **플레이스홀더로 구조만**(미션 Notes 승인).

## 배운 것 / 남은 것
- ⚠️ **"데이터 분리"의 두 단계가 다르다** — 컴포넌트 상단 `const`로 모으는 것(됨)과 "운영진이 값만 갱신하는 편집 레이어로 분리"(미완)는 별개. 지금은 전부 플레이스홀더라 랜딩이 아직 "얼굴"이 아니라 뼈대. → **후속: 실콘텐츠 수급(PM 크리티컬 패스) + 편집성(정적 vs DB 판단은 learnings의 '미디어·편집 surface' 프레임).**
