# 멋쟁이사자처럼 경희대 — 공식 소개·홍보 사이트 프로젝트

> 프로젝트 본진 / Claude 협업 하네스 · PM·조직장: 김우진(@xhae123)

## 핵심 가치 = 배운 것의 축적

이 프로젝트의 1순위 가치는 **참여 멤버의 실전 학습**이다. 그래서 하네스가 쌓는 것도 *결정 로그·진행 상태*가 아니라 **배운 것(learnings)** — 다음에도 쓸 통찰. 매 세션이 0에서 시작하지 않게 한다.

## Claude에게 — 작업 전·후

1. **시작 전** `pm/docs/learnings.md`(배운 것) → `brief.md`(프로젝트 정의)를 읽는다. 작업 디렉터리의 `CLAUDE.md`도 자동으로 함께 로드된다.
   - **코드 작업(FE·BE·인프라)이면 `pm/onboarding/dev-monorepo-ai.md`도 읽는다** — 저장소 구조·AI-native 워크플로·운영 루프(첫날·이슈사이클·막힘·PR)·용어 미니사전. *항상 로드하지 않고 개발할 때만 — 시스템 프롬프트는 가볍게(lazy load).*
   - **"사이트가 어떻게 동작해야 하나"(정책·권한·기능 명세·서버냐 코드냐)가 걸리면 서비스 문서 위키를 WebFetch 한다** → https://github.com/likelion-khu-official/website/wiki — 결정된 스펙의 정본(개요와 원칙 · 아키텍처 결정 · 정보구조와 권한 · 기능 명세). 논의 원문은 `pm/discussions/`, 판단 갈리면 위키가 이긴다. *이것도 lazy load — 관련될 때만.*
   - **보안이 걸리면(시크릿·인증·PII·취약점 등) `SECURITY.md`를 읽는다** — 공통 규칙(이 레포가 public이라는 것 등)이 있고, 거기서 다시 작업 중인 도메인의 `SECURITY.md`로 연결된다. *이것도 lazy load — 관련될 때만.*
2. **작업 끝에 *배운 것*이 생기면** `pm/docs/learnings.md`에 한 줄 남긴다(왜가 드러나게). 일회성 결정·진행 상태·기술 스택 선택 같은 건 굳이 쌓지 않는다 — 진행은 GitHub 이슈/보드로.
3. 김우진 = 의사결정자(PM·조직장), Claude = 실행·조언자. 방향은 제안하되 **결정은 김우진**.
4. 한국어. **"왜"가 핵심** — 결정·제안엔 반드시 이유를 붙인다. 단점·트레이드오프 먼저.

## 외부에 쓰는 글 — 이슈·코멘트·PR·위키

GitHub 이슈·코멘트·PR·위키처럼 **다른 사람이 읽는 글**은 이 대화의 요약이 아니라, 그 사람만 보고 이해되는 독립된 글이어야 한다.

- **받는 사람 기준으로 써라.** 이 대화를 안 본 사람이 읽는다고 가정한다 — "아까 그거", "위에서 말한 대로" 금지.
- **그 사람이 알아야 할 정보만.** 조사 과정·근거 탐색·PM 내부 용어(SOT 등)는 빼고 결론과 필요한 최소한만 남긴다.
- **왜를 반드시 붙인다.** 결론만 던지지 말고 이유를 함께 쓴다 — 이유가 없으면 받는 사람이 맹목적으로 따르게 된다.
- **쉬운 말로.** 우리끼리만 쓰는 약어나 내부 용어를 상대가 몰라도 되게 풀어 쓴다.
- **가독성.** 문단 나누고, 필요하면 소제목·리스트 쓴다. 길게 늘리지 않는다.

## 무엇을 / 왜

- **무엇:** 멋사를 외부(신입 지원자·타 동아리·기업)에 소개하는 공식 사이트.
- **왜:** 외부에 보여줄 "얼굴"이 없다. 한 학기 백엔드 스터디 자산을 동아리 자산으로 전환.
- 타겟·범위·성공기준은 `pm/docs/brief.md`.

## 팀 (8명)

GitHub 핸들은 이슈 어사인·태그용.

- **PM** 김우진 `@xhae123`
- **디자인** 김영웅 · 유한솔 — *GitHub 없음 (이슈는 카톡 등 링크로 공유)*
- **프론트엔드** 박일하 `@ParkIlha` · 김현정 `@hjdd0309`
- **백엔드** 신선우 `@sunwoo1256` · 안시현 `@xihxxn`
- **인프라** 장찬욱 `@cjang3285`

## 하네스 지도 — 어디에 뭐가 사는가

모노레포. 전원 Claude 사용(한 레포 = AI가 전체 맥락을 봄). 작업 디렉터리에서 위로 올라가며 `CLAUDE.md`가 자동 로드된다.

- **코드** → `frontend/`·`backend/`·`infra/` (각자 코드 + 역할 `CLAUDE.md`)
- **배운 것·정의** → `pm/docs/` (`learnings.md`·`brief.md`)
- **계약** → `shared/` (코드. FE↔BE API 타입. 문서로 베끼지 않는다 — drift 방지)
- **디자인** → 레포 밖 **Figma**(단일 진실). 구현은 FE가. (디자인 디렉터리 없음)
- **운영** → `pm/`
- **미션** → `pm/missions/<번호>-<슬러그>/` (제안·진행·결과 한 상자. PM쪽 정제 아카이브)
- **스펙(정본)** → GitHub **위키**(서비스 문서, `…/website/wiki`). "무엇이어야 하나". 구현·논의 전 확인. 원 논의는 `pm/discussions/` 아카이브.
- **제품 현황** → GitHub **이슈 계층(네이티브 Issue Type) + 보드**. 4층이다: **Epic**(테마 — 프로젝트 쇼케이스·블로그 등) ▸ **Story**(사용자에게 보이는 기능 한 조각 — "방문자가 ~할 수 있다") ▸ **Sub-task**(그 Story의 분야별 실작업 — 팀원이 claim해 굴린다) / **Task**(사용자 향이 아닌 순수 기술작업 — 스택 셋업·인프라). 타입은 GitHub **네이티브 Issue Type**으로 찍힌다(제목 접두어가 아니다). 하위가 sub-issue로 달려 진행률이 자동 롤업된다. 분야(BE·FE·인프라·디자인)는 **라벨**로 표시. 상태·진행은 이슈·PR·보드에서 파생 — 손으로 유지하는 상태 트리는 없다(그게 drift의 원인이었다). *예전 `pm/features/` 트리는 폐기 — 스펙은 위키로, 현황은 이슈/보드로 갈라졌다.*

> **불변식:** 코드는 포지션 디렉터리, 배운 것은 `pm/docs`, 계약은 `shared`, 미션 맥락은 `pm/missions` 박스, 팀의 실시간 협업은 GitHub. 박스는 GitHub 복붙이 아니라 PM이 정제한 맥락 — 안 섞는다.
>
> **위키(스펙) → Epic(테마) → Story(기능 슬라이스) → Sub-task/Task(전진)는 한 몸.** 위키가 "무엇이어야 하나"의 정본. **Story**가 "이 사용자 기능이 어디까지 왔나"의 추적 단위(사용자 향 수직 슬라이스 하나 = Story 하나, 예: "방문자가 프로젝트 목록을 볼 수 있다"), 그 위 **Epic**이 여러 Story를 묶는 테마(프로젝트 쇼케이스 등)다. 개발자가 claim해 R→P→I→Q로 굴리는 건 Story 밑 **Sub-task**(분야별 실작업)고, 사용자 향이 아닌 순수 기술작업(스택 셋업·인프라 등)은 **Task**다. 타입은 GitHub 네이티브 Issue Type으로 찍는다. 하위는 상위에 sub-issue로 걸리고(Epic ← Story ← Sub-task), 진행률은 GitHub이 자동 롤업한다. **백로그 = 아직 Sub-task가 안 달린 Story, 또는 위키에 스펙만 있고 Story가 없는 기능** — 새 일은 거기서 고른다. Sub-task는 얇은 하위작업이 아니라 자기 "왜"와 완료기준을 담은 리치 티켓이다(그게 개발자·AI가 사는 티켓). (운영: `mission`·`claim-mission` 스킬)
